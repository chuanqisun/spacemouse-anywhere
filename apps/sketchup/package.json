{
  "name": "sketchup-web-spacemouse",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/build.js --watch --dev",
    "build": "tsc && node scripts/build.js"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.243",
    "esbuild": "^0.19.0",
    "typescript": "^5.1.6"
  },
  "dependencies": {}
}
